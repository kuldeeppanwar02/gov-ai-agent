version: 1.0
runtime: docker
build:
  commands:
    build: docker build -t gov-ai-agent .
run:
  runtime-version: docker
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
