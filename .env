# AWS Credentials (set via AWS CLI or environment)
AWS_REGION=us-east-1

# S3 Bucket Name
S3_BUCKET_NAME=gov-ai-agent-seema-910445327304

# OpenSearch (optional, leave empty to use in-memory RAG)
OPENSEARCH_HOST=
OPENSEARCH_REGION=us-east-1

# CORS (comma-separated origins, use * for all)
CORS_ORIGINS=http://localhost:3000
